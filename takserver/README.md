# TAK Server

This part runs the map and live position server.

It receives drone positions and shares them with TAK clients. The goal is to give the team one place to see the situation.

Copy `.env.example` to `.env` and set your own secret values before running it.
